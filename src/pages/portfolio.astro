---
import MainLayout from "../layouts/MainLayout.astro";
import MoreAboutMe from "../components/MoreAboutMe.astro";
import Tool from "../components/Tool.astro";
import Projects from "../components/Projects.astro";
import { motion, AnimatePresence } from "framer-motion";
import RotatingText from "../components/RotatingText.jsx";
import Bubble from "../components/Bubble.astro";
import Hero from "../components/Hero.astro";
---

<MainLayout title="About Me">
  <!-- ===================== HERO ===================== -->
 <section class="margin:0; background:black; overflow:hidden;"><Bubble></Bubble></section>

  <!-- ===================== SECTION A ===================== -->
  <section 
    x-data="{ 
      inView: false,
      y: 0
    }"
    x-init="
      const observer = new IntersectionObserver((entries) => {
        inView = entries[0].isIntersecting;
      }, { threshold: 0.3 });
      observer.observe($el);
    "
    @scroll.window.passive="y = window.scrollY || 0"
    class="mx-auto max-w-6xl px-5 md:px-6 py-14 md:pt-28"
  >
  <div class="grid grid-cols-1 md:grid-cols-12 gap-8 md:gap-12 items-start">
  <!-- Text -->
    <div class="md:col-span-6 text-left">
      <h2 
      x-show="inView"
      x-transition:enter="transition ease-out duration-800 delay-100"
      x-transition:enter-start="opacity-0 transform translate-y-8"
      x-transition:enter-end="opacity-100 transform translate-y-0"
      class=" text-3xl md:text-4xl font-bold font-title">
      Nice to meet you.
      </h2>

    <!-- ✅ Rotating Text with Framer Motion -->
    <div class="flex flex-wrap items-baseline text-3xl md:text-4xl font-bold tracking-tight font-title leading-tight">
      <span>I am a&nbsp;</span>

      <!-- Rotating Text -->
      <RotatingText
        client:load
        texts={['Digital', 'UI/UX', 'Concept', 'Interactive']}
        mainClassName="text-accent-50 font-semibold inline-block align-baseline"
        staggerFrom="last"
        initial={{ opacity: 0, y: "20%" }}
        animate={{ opacity: 1, y: "0%" }}
        exit={{ opacity: 0, y: "-20%" }}
        staggerDuration={0.025}
        splitLevelClassName="inline-block align-baseline"
        transition={{ duration: 0.6, ease: 'easeInOut' }}
        rotationInterval={1800}
      />
      <span>&nbsp;Designer.</span>
    </div>

    <p class="text-3xl md:text-4xl font-bold eading-tight font-title">
     Driven by strategy and empathy, I design innovative solutions — analyzing problems, exploring ideas, and shaping meaningful experiences.
    </p>
      <!-- ===================== MORE ABOUT ME (Parallax + Folder Stack) ===================== -->
    <div class="py-4 md:py-6">
      <MoreAboutMe></MoreAboutMe>
    </div>
  </div>

  <!-- Profile image -->
  <div class="md:col-span-6">
    <div 
      x-show="inView"
      x-transition:enter="transition ease-out duration-1000 delay-400"
      x-transition:enter-start="opacity-0 transform translate-y-12 scale-95"
      x-transition:enter-end="opacity-100 transform translate-y-0 scale-100"
      class="overflow-hidden rounded-xl border border-black/10 shadow-md will-change-transform"
    >
      <img src="/profil.png" alt="Profile" class="w-full h-auto object-cover" />
    </div>
  </div>
</div>

  </section>

  <!-- ===================== SECTION B ===================== -->
  <section 
    x-data="{ inView: false }"
    x-init="
      const observer = new IntersectionObserver((entries) => {
        inView = entries[0].isIntersecting;
      }, { threshold: 0.2 });
      observer.observe($el);
    "
    class="bg-primary-50/50"
  >
    <div class="mx-auto max-w-6xl px-5 md:px-6 py-16 md:py-28">
      <h3 
        x-show="inView"
        x-transition:enter="transition ease-out duration-800"
        x-transition:enter-start="opacity-0 transform translate-y-8"
        x-transition:enter-end="opacity-100 transform translate-y-0"
        x-transition:leave="transition ease-in duration-400"
        x-transition:leave-start="opacity-100 transform translate-y-0"
        x-transition:leave-end="opacity-0 transform translate-y-8"
        class="mb-6 md:mb-10 text-2xl  max-w-4xl md:text-4xl font-extrabold "
      >
        I design experience-driven concepts combining intuition and storytelling
      </h3>

      <div class="grid gap-8 md:grid-cols-3 md:py-8">
        <!-- Card 1 -->
        <div 
          x-show="inView"
          x-transition:enter="transition ease-out duration-600 delay-100"
          x-transition:enter-start="opacity-0 transform translate-y-12 scale-95"
          x-transition:enter-end="opacity-100 transform translate-y-0 scale-100"
          x-transition:leave="transition ease-in duration-300"
          x-transition:leave-start="opacity-100 transform translate-y-0 scale-100"
          x-transition:leave-end="opacity-0 transform translate-y-12 scale-95"
          class="rounded-2xl bg-white p-6 shadow-sm hover:shadow-lg hover:-translate-y-2 transition-all duration-300"
        >
          <div class="mx-auto mb-4 h-20 w-20">
            <img src="/doublediamond.svg" alt="Double Diamond" class="w-20 h-20 scale-[1.4]"/>
          </div>
          <h4 class="mb-2 text-lg font-semibold text-center">Design Thinking</h4>
          <p class="text-center text-sm leading-6 ">
            I base my mindset on design thinking, approaching every challenge with structured yet creative methods.
          </p>
        </div>

        <!-- Card 2 -->
        <div 
          x-show="inView"
          x-transition:enter="transition ease-out duration-600 delay-200"
          x-transition:enter-start="opacity-0 transform translate-y-12 scale-95"
          x-transition:enter-end="opacity-100 transform translate-y-0 scale-100"
          x-transition:leave="transition ease-in duration-300"
          x-transition:leave-start="opacity-100 transform translate-y-0 scale-100"
          x-transition:leave-end="opacity-0 transform translate-y-12 scale-95"
          class="rounded-2xl bg-white p-6 shadow-sm hover:shadow-lg hover:-translate-y-2 transition-all duration-300"
        >
          <div class="mx-auto mb-4 h-20 w-20">
            <img src="/ideas.svg" alt="Creative" class="w-20 h-20"/>
          </div>
          <h4 class="mb-2 text-lg font-semibold  text-center">Creative Approach</h4>
          <p class="text-center text-sm leading-6 ">
            I break mental barriers and push ideas beyond the obvious to explore innovative possibilities.
          </p>
        </div>

        <!-- Card 3 -->
        <div 
          x-show="inView"
          x-transition:enter="transition ease-out duration-600 delay-300"
          x-transition:enter-start="opacity-0 transform translate-y-12 scale-95"
          x-transition:enter-end="opacity-100 transform translate-y-0 scale-100"
          x-transition:leave="transition ease-in duration-300"
          x-transition:leave-start="opacity-100 transform translate-y-0 scale-100"
          x-transition:leave-end="opacity-0 transform translate-y-12 scale-95"
          class="rounded-2xl bg-white p-6 shadow-sm hover:shadow-lg hover:-translate-y-2 transition-all duration-300"
        >
          <div class="mx-auto mb-4 h-20 w-20">
            <img src="/heart.svg" alt="Human-Centered" class="w-20 h-20"/>
          </div>
          <h4 class="mb-2 text-lg font-semibold text-center">Human-Centered</h4>
          <p class="text-center text-sm leading-6 ">
            I always design for people, ensuring solutions that are intuitive, functional, and meaningful.
          </p>
        </div>
      </div>
    </div>
  </section>
  <!-- ===================== TOOLS SECTION ===================== -->
  <Tool></Tool>
  <!-- ===================== PROJECTS SECTION ===================== -->
   <div id="Card"> <Projects></Projects></div>
 
 <!-- ===================== CONTACT SECTION ===================== -->
<section class="relative flex flex-col items-center justify-center min-h-screen bg-black overflow-hidden px-6">
  <h1 id="gradient-text"
      class="font-extrabold text-center leading-tight tracking-tight select-none block mx-auto"
      style="
        font-size: clamp(2rem, 6vw, 6rem);
        color: transparent;
        background-image: radial-gradient(circle at center, #ff9966 0%, #feb47b 40%, #ffffff 80%);
        -webkit-background-clip: text;
        background-clip: text;
        background-repeat: no-repeat;
        background-size: 1200px 1200px;
        transition: background-position 0.05s linear;
        max-width: 18ch; /* ✅ 문장 길이 제한 */
        text-wrap: balance; /* ✅ 문장 길이에 따라 자연스러운 줄바꿈 */
        padding: 0 1rem;
      ">
    Every connection begins with curiosity.
  </h1>
  <div class="flex justify-between gap-56 max-auto max-w-6xl text-white pt-8">
    <p>skoghj@gmail.com</p>
    <p>+45 50 38 47 43</p>
     <a href="https://www.linkedin.com/in/huijin-bak/">LinkedIn</a>
  </div>
  
  
</section>
</MainLayout>
<script>
  const text = document.getElementById("gradient-text");

  window.addEventListener("mousemove", (e) => {
    const x = e.clientX / window.innerWidth * 100;
    const y = e.clientY / window.innerHeight * 100;

    text.style.backgroundImage = `
      radial-gradient(circle at ${x}% ${y}%,
        #ff9966 0%, #feb47b 40%, #ffffff 80%)
    `;
  });
</script>









