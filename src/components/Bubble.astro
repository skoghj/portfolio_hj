---
import MetaBalls from "../components/MetaBalls.jsx";
import HeroText from "./HeroText.jsx";
---

<section class="relative w-full h-[600px] flex items-center justify-center bg-white overflow-hidden">
  <!-- React 컴포넌트를 클라이언트에서 실행 -->
  <MetaBalls client:load />
  <!-- 중앙에 텍스트 회전 -->
  <div class="absolute flex items-center justify-center">
    <HeroText client:load />
  </div>
</section>
